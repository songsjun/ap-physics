# 开发上下文

**更新日期**: 2026-05-21  
**用途**: 供后续开发会话快速恢复上下文，避免重复决策

---

## 当前项目状态

**已完成并可运行**：学习主界面（DayListView）含完整资源列表、A/B/C 三层分组、历年 FRQ 推荐区、AI 每日反馈、进度仪表盘、设置页。

**未完成**：原生 MC 题（QuizCard）、AI 自检评分（AIGradedCard）、只读分享链接（V2）。

---

## 实际目录结构

```
ap-physics-app/
├── app/
│   ├── layout.tsx                    # AppInitializer + 全局布局
│   ├── page.tsx                      # 首页（跳转当前进度）
│   ├── week/[w]/day/[d]/
│   │   ├── page.tsx                  # generateStaticParams（56个页面）
│   │   ├── DayPageClient.tsx         # DayProvider 客户端包裹
│   │   └── DayHeader.tsx
│   ├── dashboard/
│   │   ├── page.tsx
│   │   └── DashboardClient.tsx
│   └── settings/
│       ├── page.tsx
│       └── SettingsClient.tsx
│
├── components/
│   ├── DayListView.tsx               # 核心学习界面（最重要的组件）
│   ├── DayView.tsx                   # FlowState switch → 渲染决策
│   ├── AppInitializer.tsx            # DB seed + storage.persist + 健康检查
│   ├── DaySkeleton.tsx
│   └── adapters/
│       └── ExternalCard.tsx          # external_manual（已实现，其余 TODO）
│
├── lib/
│   ├── types.ts                      # 所有共享类型
│   ├── constants.ts                  # PASS_THRESHOLD=0.75, LIBRARY_VERSION='1.4.0', WEEKS=8, DAYS_PER_WEEK=7
│   ├── db.ts                         # Dexie schema（getDb() 懒初始化）
│   ├── storage.ts                    # StorageService: userId + apiKey
│   ├── queries.ts                    # seedContentLibrary（版本化，派生 adapter_type/phase/slot_order）
│   ├── frq.ts                        # findRelatedFRQ / frqTypeLabel / FRQEntry 类型
│   ├── ai.ts                         # Claude API 封装
│   ├── repository/
│   │   ├── interface.ts              # IRepository 接口
│   │   ├── dexie.repo.ts             # IndexedDB 实现
│   │   └── index.ts                  # export const repo
│   ├── domain/
│   │   ├── flow.ts                   # computeFlowState（纯函数）+ shouldUnlock
│   │   ├── adaptive.ts               # prioritize（纯函数）
│   │   ├── catalog.ts                # ContentCatalog（只读查询）
│   │   └── progress.ts               # ProgressTracker（读写）
│   └── app/
│       ├── snapshot.ts               # assembleDaySnapshot
│       ├── session.ts                # DaySessionManager
│       ├── session-context.tsx       # DayProvider + useDayContext
│       └── share.ts                  # exportJson
│
├── data/
│   ├── content_library.json          # 270 条资源 + 49 个知识点（LIBRARY_VERSION=1.4.0）
│   └── frq_map.json                  # 28 道历年 FRQ 知识点映射
│
└── public/frq/                       # 历年 FRQ PDF（本地，约 12MB 合计）
    ├── ap19-frq-physics-1.pdf / ap19-sg-physics-1.pdf
    ├── ap22-frq-physics-1.pdf / ap22-sg-physics-1.pdf
    ├── ap23-frq-physics-1.pdf / ap23-sg-physics-1.pdf
    ├── ap24-frq-physics-1.pdf / ap24-sg-physics-1.pdf
    ├── ap25-frq-physics-1.pdf / ap25-sg-physics-1.pdf
    └── ap26-frq-physics-1.pdf        # SG 暂缺（CollegeBoard 尚未发布）
```

---

## 内容库现状

| 类型 | 数量 | 说明 |
|------|------|------|
| A 层资源 | 100 | Khan / PhET / AP Central / 原生题 |
| B 层资源 | 114 | OpenStax 阅读 + Test Prep |
| C 层资源 | 56 | OpenStax Problems & Exercises |
| 知识点 (KP) | 49 | 覆盖 8 个 Unit，35 个 CED Topics |
| FRQ 题目 | 28 | 2019、2022–2026，含 concepts[] 映射 |

`LIBRARY_VERSION = '1.4.0'` — content_library.json 变动时必须升版本号触发重 seed。

---

## 核心设计决策（不要重新讨论）

| 决策 | 结论 |
|------|------|
| 数据存储 | IndexedDB via Dexie.js 4.4.2（OPFS 已评估排除）|
| 数据访问 | Repository 模式（IRepository 接口），组件不直接用 Dexie |
| 部署 | 纯静态导出（output: 'export'）→ Cloudflare Pages / GitHub Pages |
| AI API Key | 用户在设置页输入，存 localStorage，不上传任何服务器 |
| Claude API 调用 | 浏览器直调（dangerouslyAllowBrowser: true）|
| PDF 展示 | `url#page=N` fragment 浏览器新标签页打开，不用 PDF.js embed |
| Khan 验证 | 手动输入得分，不做截图 AI 解析 |
| FRQ 推荐 | 按 concepts[] 交集得分排序，取前 6 个，score 相同时年份降序 |
| 多设备同步 | MVP 不支持，导出/导入 JSON 作为 V2 方案 |

---

## FRQ 系统说明

**frq_map.json 结构**：
```json
{
  "id": "ap25-q1",
  "year": 2025,
  "question_number": 1,
  "frq_type": "type1_mathematical",
  "frq_pdf": "/frq/ap25-frq-physics-1.pdf",
  "frq_page": 3,
  "sg_pdf": "/frq/ap25-sg-physics-1.pdf",
  "sg_page": 2,
  "concepts": ["momentum-conservation", "momentum-collisions"],
  "text_preview": "..."
}
```

**PDF 页码来源**：pdftotext 提取后按年份特定 regex 定位（2025-2026 用 `Question N:`，2022-2024 用 `Begin your response to QUESTION N`，2019 用 `N. (X points`）。

**DayListView 中的 FRQ 区域**：
- 可折叠（同 TierSection 风格）
- 点击题目 → 新标签页打开 PDF，定位到目标页
- 点击答案 → 先弹出「道德审判」对话框（确认已独立完成），再打开 SG PDF
- ap26 SG 目前外链到 AP Central（本地文件待 CollegeBoard 发布后替换）

---

## 关键实现细节

### seedContentLibrary（queries.ts）
- 版本检查：读 `db.meta` 的 `content_version`，等于 `LIBRARY_VERSION` 则跳过
- 派生字段：`adapter_type`（从 type/platform 推断）、`phase`（LEARN/PRACTICE/CHECK）、`slot_order`（按 week-day-tier 分组后排序）
- 全部在同一 Dexie 事务内写入，成功后写 meta

### DayListView 的两个 Effect
- **Effect A**（依赖 `[week, day]`）：加载 resources + kpMap，含 cancelled flag
- **Effect B**（依赖 `[week, day, flowState]`）：刷新 completions Map，含 cancelled flag
- 两个 effect 分离，避免 flowState 变化触发 resources 重查

### B 层自动展开
- `hasAFailed = aTier.some(r => completions.get(r.id)?.status === 'failed')`
- B 层 TierSection 接收 `forceOpen={hasAFailed}`，首次失败时自动展开

### ScorePanel 默认值
- `useState(0)` 而非 `useState(max)`，避免学生不输入分数直接提交

---

## 8 周内容分布

| 周 | CED Unit | 主题 | KP 数 | A/B/C 资源 |
|----|---------|------|-------|-----------|
| Week 1 | U1 | Kinematics | 5 | 14/17/9 |
| Week 2 | U2 | Newton's Laws 基础 | 6 | 10/7/6 |
| Week 3 | U2 | 斜面 / 圆周 / 引力 | 6 | 9/6/6 |
| Week 4 | U3 | Work, Energy, Power | 7 | 13/13/6 |
| Week 5 | U4 | Linear Momentum | 5 | 10/12/5 |
| Week 6 | U5–6 | Torque + Rotation | 12 | 13/5/8 |
| Week 7 | U7–8 | Oscillations + Fluids | 8 | 10/7/7 |
| Week 8 | 综合 | 复习 + 弱点强化 | — | 21/47/9 |

---

## 已排除方案（不要重新提议）

- Pipeline 独立数据表（week/day/slot_order 已够）
- iframe 嵌入 OpenStax（X-Frame-Options 未验证，外链更保险）
- PDF.js embed 渲染 FRQ（浏览器原生 PDF 新标签页体验更好）
- FRQ 全文提取为 HTML（pdftohtml 产出依赖绝对定位，方程式不可选中）
- OPFS 替代 IndexedDB（无原生查询，SQLite WASM 需 COOP/COEP 破坏外链）
- Edge Function / Vercel（零服务端代码）

---

## 已知遗留事项（未来可做）

- **queries.ts TOCTOU**：多标签同时打开时 seedContentLibrary 可能并发写入，低优先级
- **QuizCard / AIGradedCard**：适配器未实现（native_quiz、ai_graded_text 资源在 UI 中暂不可用）
- **ap26 SG**：CollegeBoard 尚未发布，答案链接为外链，发布后替换为本地文件
- **DayListView 拆分**：目前 ~600 行，未来可拆分为更小的组件文件
