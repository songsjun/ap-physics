# 开发上下文

**更新日期**: 2026-05-22  
**用途**: 供后续开发会话快速恢复上下文，避免重复决策

---

## 当前项目状态

**已完成并可运行**：学习主界面（DayListView）含完整资源列表、A/B/C 三层分组、历年 FRQ 推荐 + 分数回填、每日挑战题（Quiz Panel）、每日综合得分（0–100）、AI 每日反馈、进度仪表盘（含分数 badge）、设置页。

**未完成（V2）**：只读分享链接、多设备同步。

---

## 实际目录结构

```
ap-physics-app/
├── app/
│   ├── layout.tsx                    # AppInitializer + 全局布局
│   ├── page.tsx                      # 首页（跳转当前进度）
│   ├── week/[w]/day/[d]/
│   │   ├── page.tsx                  # generateStaticParams（56个页面）
│   │   └── DayPageClient.tsx         # DayProvider 客户端包裹
│   ├── dashboard/
│   │   ├── page.tsx
│   │   └── DashboardClient.tsx       # DayCell + 分数 badge
│   └── settings/
│       ├── page.tsx
│       └── SettingsClient.tsx
│
├── components/
│   ├── AppInitializer.tsx            # DB seed + storage.persist + 健康检查
│   ├── DaySkeleton.tsx
│   └── day/
│       ├── DayListView.tsx           # 核心学习界面（最重要的组件，~350行）
│       ├── DayHeader.tsx
│       ├── ResourceRow.tsx           # TierSection + RowSharedProps
│       ├── ChallengePrompt.tsx       # 挑战提示卡
│       ├── QuizPanel.tsx             # Quiz 主流程（MCQ/fill/short/feynman）
│       └── RelatedFRQCard.tsx        # FRQ 推荐 + 分数回填 + 道德审判弹窗
│
├── lib/
│   ├── types.ts                      # 所有共享类型（含 FRQCompletion、QuizResult）
│   ├── constants.ts                  # PASS_THRESHOLD / LIBRARY_VERSION / QUIZ_BANK_VERSION 等
│   ├── infra/
│   │   ├── db.ts                     # Dexie schema（getDb() 懒初始化）v4含frq_completions表
│   │   ├── storage.ts                # StorageService: userId + apiKey
│   │   ├── seed.ts                   # seedContentLibrary（版本化）
│   │   ├── seed-quiz.ts              # seedQuizBank（版本化）
│   │   └── ai.ts                     # Claude API 封装（gradeAnswer / chat / getDailyFeedback）
│   ├── repository/
│   │   ├── interface.ts              # IRepository 接口（含 FRQ completion 方法）
│   │   ├── dexie.repo.ts             # IndexedDB 实现
│   │   └── index.ts                  # export const repo
│   ├── domain/
│   │   ├── flow.ts                   # computeFlowState（纯函数）+ shouldUnlock
│   │   ├── adaptive.ts               # prioritize（纯函数）
│   │   ├── catalog.ts                # ContentCatalog（只读查询）
│   │   ├── progress.ts               # ProgressTracker（读写）
│   │   ├── frq.ts                    # findRelatedFRQ / frqTypeLabel / FRQEntry 类型
│   │   └── scoring.ts                # computeDayScore（纯函数）→ 0–100 每日得分
│   └── app/
│       ├── snapshot.ts               # assembleDaySnapshot
│       ├── session.ts                # DaySessionManager
│       ├── session-context.tsx       # DayProvider + useDayContext
│       ├── useDayResources.ts        # 资源/完成度/挑战状态 hook（DayListView 专用）
│       ├── quiz.ts                   # selectDailyQuestions（去重 + 薄弱概念加权）
│       ├── ready.ts                  # isAppReady 检查
│       └── share.ts                  # exportJson
│
├── data/
│   ├── content_library.json          # 270 条资源 + 49 个知识点（LIBRARY_VERSION=1.4.0）
│   ├── quiz-bank.json                # 197 题（147 regular + 50 feynman，QUIZ_BANK_VERSION）
│   └── frq_map.json                  # 28 道历年 FRQ 知识点映射
│
├── tests/
│   ├── domain/flow.test.ts           # computeFlowState 单元测试（22 个 case）
│   ├── domain/adaptive.test.ts
│   ├── app/session.test.ts
│   ├── app/share.test.ts
│   └── data/quiz-content.test.ts
│
└── public/frq/                       # 历年 FRQ PDF（本地，约 12MB）
    ├── ap19-frq / ap22-frq / ap23-frq / ap24-frq / ap25-frq / ap26-frq -physics-1.pdf
    └── ap19-sg / ap22-sg / ap23-sg / ap24-sg / ap25-sg -physics-1.pdf
    # ap26 SG 暂缺（CollegeBoard 尚未发布，答案链接为外链）
```

---

## 内容库现状

| 类型 | 数量 | 说明 |
|------|------|------|
| A 层资源 | 100 | Khan / PhET / AP Central / 原生题 |
| B 层资源 | 114 | OpenStax 阅读 + Test Prep |
| C 层资源 | 56 | OpenStax Problems & Exercises |
| 知识点 (KP) | 49 | 覆盖 8 个 Unit，35 个 CED Topics |
| FRQ 题目 | 28 | 2019、2022–2026，含 concepts[] 映射 |
| Quiz 题库 | 197 | MCQ/fill/short/feynman，difficulty 1–3 |

版本标记：
- `LIBRARY_VERSION = '1.4.0'` — content_library.json 变动时升版本触发重 seed
- `QUIZ_BANK_VERSION` — quiz-bank.json 变动时升版本

---

## 核心设计决策（不要重新讨论）

| 决策 | 结论 |
|------|------|
| 数据存储 | IndexedDB via Dexie.js 4.4.2（OPFS 已评估排除）|
| 数据访问 | Repository 模式（IRepository 接口），组件不直接用 Dexie |
| 部署 | 纯静态导出（output: 'export'）→ Cloudflare Pages / GitHub Pages |
| AI API Key | 用户在设置页输入，存 localStorage，不上传任何服务器 |
| Claude API 调用 | 浏览器直调（CORS 模式，dangerousRequestType: CORS 头）|
| PDF 展示 | `url#page=N` fragment 浏览器新标签页打开，不用 PDF.js embed |
| Khan/练习验证 | 手动输入得分，不做截图 AI 解析 |
| FRQ 推荐 | 按 concepts[] 交集得分排序，取前 6 个，score 相同时年份降序 |
| 多设备同步 | MVP 不支持，导出/导入 JSON 作为 V2 方案 |
| Quiz 客观题评分 | MCQ/fill 本地即时评分，不调 AI；short/feynman 才调 AI |
| FRQ 分数回填 | 学生自行输入得分/满分，系统不做 AI 评分 |
| 每日得分 | 纯函数 computeDayScore()，输入完成度快照，输出 0–100 综合分 |

---

## 每日综合得分设计（lib/domain/scoring.ts）

每日得分 = A层分 + B层加成 + C/FRQ加成 + Quiz分，最高 100 分。

| 维度 | 满分 | 算法 |
|------|------|------|
| A 层完成 | 30 | `completionRate × 30`（完成数 / A总数）|
| A 层质量 | 25 | `passRate × 25`（通过数 / 已批改数）|
| B 层加成 | 7 | `bQuality × min(bGraded/3, 1) × 7` |
| C+FRQ加成 | 8 | `cQuality × min((cGraded+frqCount)/3, 1) × 8` |
| Quiz | 30 | `(deduped correct rate)² × 30`（平方放大错误惩罚）|

校准锚点：
- A全对 + Quiz全对 = 85
- A全对 + Quiz答错1题（feynman错）≈ 70
- A+B+C + Quiz全对 = 100

Quiz 权重：feynman=0.30 > difficulty-3=0.25 > difficulty-2=0.20 > default=0.15  
时效系数：7天内=1.0，30天内=0.85，更早=0.70（只影响 Quiz 权重，不影响完成度）

---

## FRQ 系统说明

**frq_map.json 结构**：
```json
{
  "id": "ap25-q1",
  "year": 2025,
  "question_number": 1,
  "frq_type": "type1_mathematical",
  "frq_pdf": "/frq/ap25-frq-physics-1.pdf",
  "frq_page": 3,
  "sg_pdf": "/frq/ap25-sg-physics-1.pdf",
  "sg_page": 2,
  "concepts": ["momentum-conservation", "momentum-collisions"],
  "text_preview": "..."
}
```

**FRQ 分数回填**：`FRQCompletion` 表存 `{ user_id, frq_id, week, day, score, score_max, completed_at }`，`week/day` 在回填时按当前页上下文写入（不从 concepts 推导）。通过标准 ≥ 60%。

**PDF 页码来源**：pdftotext 提取后按年份特定 regex 定位（2025-2026 用 `Question N:`，2022-2024 用 `Begin your response to QUESTION N`，2019 用 `N. (X points`）。

**DayListView 中的 FRQ 区域**：
- 可折叠（同 TierSection 风格）
- 点击题目 → 新标签页打开 PDF，定位到目标页
- 点击答案 → 先弹出「道德审判」对话框（确认已独立完成），再打开 SG PDF，自动进入分数录入
- 「记录分数 / 修改分数」按钮，内联输入得分/满分后保存

---

## Quiz 系统说明

**selectDailyQuestions**（lib/app/quiz.ts）：
- 3 道普通题 + 1 道 feynman 题 = 4 题/天
- 全局去重（当前用户已做过的题不再出）
- feynman 题：优先选薄弱概念（A 层失败资源的 concepts）
- 普通题：覆盖当天 A 层 concepts，difficulty 均衡

**QuizPanel 评分逻辑**（components/day/QuizPanel.tsx）：
- MCQ / fill：本地即时评分（`gradeLocal()`），立即跳结果页，DB 保存 fire-and-forget
- short / feynman：AI 批改，15s 超时，超时时返回 fallback 不写入结果
- 结果页后可进入 AI 追问 chat（`AIService.chat()`，不依赖 API Key 时有提示）

**mountedRef StrictMode 修复**（重要）：  
`useEffect(() => { mountedRef.current = true; return () => { mountedRef.current = false } }, [])`  
原因：React StrictMode 开发模式下 effect cleanup 先于 setup 运行，导致 `mountedRef.current = false` 即使组件已挂载。

---

## 关键实现细节

### seedContentLibrary（lib/infra/seed.ts）
- 版本检查：读 `db.meta` 的 `content_version`，等于 `LIBRARY_VERSION` 则跳过
- 派生字段：`adapter_type`（从 type/platform 推断）、`phase`（LEARN/PRACTICE/CHECK）、`slot_order`（按 week-day-tier 分组后排序）
- 全部在同一 Dexie 事务内写入，成功后写 meta
- seedContentLibrary + seedQuizBank 并行（Promise.all，首次加载省 ~300ms）

### useDayResources（lib/app/useDayResources.ts）
- **Effect A**（依赖 `[week, day]`）：加载 resources + kpMap + 检查既有 quiz results / available questions
- **Effect B**（依赖 `[week, day, flowState]`）：刷新 completions Map
  - **重要**：依赖 `flowState`（整个对象）而非 `flowState.phase`，因为 B/C 层完成不改变 phase

### B 层自动展开
- `hasAFailed = aTier.some(r => completions.get(r.id)?.status === 'failed')`
- B 层 TierSection 接收 `forceOpen={hasAFailed}`，首次失败时自动展开

### ScorePanel 默认值
- `useState(0)` 而非 `useState(max)`，避免学生不输入分数直接提交

### Dashboard 得分 badge
- DashboardClient 预加载 `getAllFRQCompletionsForUser`，按 `week/day` 分组
- 每个 DayCell 调用 `computeDayScore()` 得出 0–100 分，显示为右上角色彩分级 badge
- 颜色分级：≥80=emerald，≥60=blue，≥40=amber，<40=red，null=无 badge

---

## 8 周内容分布

| 周 | CED Unit | 主题 | KP 数 | A/B/C 资源 |
|----|---------|------|-------|-----------|
| Week 1 | U1 | Kinematics | 5 | 14/17/9 |
| Week 2 | U2 | Newton's Laws 基础 | 6 | 10/7/6 |
| Week 3 | U2 | 斜面 / 圆周 / 引力 | 6 | 9/6/6 |
| Week 4 | U3 | Work, Energy, Power | 7 | 13/13/6 |
| Week 5 | U4 | Linear Momentum | 5 | 10/12/5 |
| Week 6 | U5–6 | Torque + Rotation | 12 | 13/5/8 |
| Week 7 | U7–8 | Oscillations + Fluids | 8 | 10/7/7 |
| Week 8 | 综合 | 复习 + 弱点强化 | — | 21/47/9 |

---

## 已排除方案（不要重新提议）

- Pipeline 独立数据表（week/day/slot_order 已够）
- iframe 嵌入 OpenStax（X-Frame-Options 未验证，外链更保险）
- PDF.js embed 渲染 FRQ（浏览器原生 PDF 新标签页体验更好）
- FRQ 全文提取为 HTML（pdftohtml 产出依赖绝对定位，方程式不可选中）
- OPFS 替代 IndexedDB（无原生查询，SQLite WASM 需 COOP/COEP 破坏外链）
- Edge Function / Vercel（零服务端代码）
- ChecklistCard / QuizCard / AIGradedCard 组件（统一由 QuizPanel 承载）
- FRQ AI 评分（自动批改 FRQ 质量不可控，回填得分更可信）

---

## 已知遗留事项（未来可做）

- **ap26 SG**：CollegeBoard 尚未发布，答案链接为外链，发布后替换为本地文件
- **quiz_results 无限增长**：重复刷题会积累，未来可加保留策略（如每题保留最近 N 条）
- **difficulty-3 题目偏少**：Units 2–8 高难度题仍不足
- **DayListView**：组件约 350 行，未来可拆分为更小单元（已可用，低优先级）
- **多设备同步**：V2 导出/导入 JSON（已有 exportJson）
- **seed TOCTOU**：多标签同时打开时 seed 可能并发写入，bulkPut 幂等不丢数据，低频低风险
