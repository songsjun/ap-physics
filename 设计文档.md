# AP Physics 1 自适应学习平台 — 设计文档

**版本**: v0.5  
**日期**: 2026-05-29  
**状态**: MVP 完成可运行，已部署 Cloudflare Pages

---

## 一、产品定位

把分散在 Khan Academy、OpenStax、PhET、Flipping Physics、AP Central 五个平台上的 AP Physics 1 学习计划，整合成一个**有序、可追踪、有反馈的完整学习体验**。

平台不重建内容，而是作为**学习编排器**：控制顺序、收集结果、提供反馈、驱动自适应。

---

## 二、教育核心逻辑

### 两轮学习制

- **第一轮**（当前系统目标）：完成全部知识点学习，达到 **75% 掌握程度**
- **第二轮**（系统外）：针对第一轮薄弱点加强补全；系统提供弱点数据，不主导执行

### 三层任务分级

| 层级 | 触发条件 | 计入掌握度 | 内容来源 |
|------|---------|-----------|---------|
| **A 必做** | 始终显示 | ✅ 是 | Khan / PhET / 原生题 / FRQ |
| **B 建议** | A 层通过率 < 75% 时自动显示 | ❌ 否 | OpenStax 阅读 + Test Prep |
| **C 拓展** | 始终隐藏，手动展开 | ❌ 否 | OpenStax Problems & Exercises |

### 每日学习循环

```
LEARN → PRACTICE → CHECK → FEEDBACK
  ↑                            ↓
  └────── 未达标时重新触发 ──────┘
```

- **LEARN**：视频 / 阅读 / 实验，点击确认完成
- **PRACTICE**：Khan 练习，手动输入得分
- **CHECK**：原生 MC 题 + AI 评分自检题，平台内完成
- **FEEDBACK**：AI 根据当天得分自动生成：今日强项 / 注意点 / 明日预习建议

---

## 三、用户角色

**学生（主用户）**：注册账号，按自己节奏完成学习任务，查看个人仪表盘。

**只读观察者**：学生生成一个 UUID 分享链接，持有者看到与学生完全相同的视图，无写权限，无需注册账号。学生可随时撤销重生成。

---

## 四、内容架构

### 知识点图谱

50 个知识点节点，涵盖 AP Physics 1 全部 8 个 Unit，35 个 CED Topics。

每个知识点包含：
- CED Topic 编号（如 `1.3`）、中英文名称
- 所在 Week / Day
- 前置知识点列表 `prerequisites[]`
- 对应 OpenStax 章节 `openstax_sections[]`

### 内容库

289 条资源（`data/content_library.json`），三层分布：

| 层级 | 数量 | 平台分布 |
|------|------|---------|
| A 层 | 113 | Khan 94 / PhET 14 / AP Central 2 / OpenStax 3 |
| B 层 | 119 | OpenStax 阅读 + Test Prep |
| C 层 | 57 | OpenStax Problems & Exercises |

另有 `data/frq_map.json`：28 道历年 FRQ 知识点映射（2019、2022–2026 年），与内容库资源通过 `concepts[]` 关联。

每条资源携带 `concepts[]` 标签，是自适应推荐的查询依据。

### CED 覆盖状态（Fall 2024）

| 状态 | 数量 |
|------|------|
| ✅ 完全覆盖 | 36 |
| ⚠️ 部分覆盖（有补缺方案） | 7 |
| ❌ 完全缺口 | 0 |

中风险缺口：Topic 5.5（转动平衡，Khan 无覆盖）、Topic 7.3（SHM 图像，AP 高频考点）。

---

## 五、技术架构

### 核心原则：三层分离

```
┌─────────────────────────────────────┐
│         Flow Controller             │  ← 唯一的「下一步做什么」决策者
├─────────────────────────────────────┤
│      Learning Pipeline              │  ← 顺序 + 门控，存在数据里
├─────────────────────────────────────┤
│      Resource Adapter               │  ← 4 种交互模式，switch 实现
├─────────────────────────────────────┤
│      Completion Signal              │  ← 所有平台的结果统一结构
└─────────────────────────────────────┘
```

每层独立变化：换资源不改流程，加平台不改逻辑，改门控不改适配器。

数据访问通过 **Repository 模式（IRepository）** 抽象——组件只调用 `repo.xxx()`，不直接依赖 Dexie.js。未来迁移至 PostgreSQL 只替换实现（`DexieRepository` → `ApiRepository`），组件代码零改动。

---

### 数据模型（IndexedDB，Dexie v4）

IndexedDB 数据库名 `PhysicsLearningDB`，当前 schema version 4。完整定义见 `lib/infra/db.ts`，类型见 `lib/types.ts`。

| 表 | 主键 | 用途 |
|----|------|------|
| `resources` | `id` | 静态内容，从 content_library.json seed |
| `knowledge_points` | `id` | 知识点图谱，从 content_library.json seed |
| `completions` | `[user_id+resource_id]` | A/B/C 层资源完成记录 |
| `day_unlocks` | `[user_id+week+day]` | 每天解锁状态 |
| `quiz_questions` | `id` | Quiz 题库，从 quiz-bank.json seed |
| `quiz_results` | `id` | 答题记录，支持重复作答（取最新） |
| `frq_completions` | `[user_id+frq_id]` | FRQ 得分回填记录 |
| `meta` | `key` | 版本控制（content_version / quiz_bank_version）|

用户 ID 为 localStorage 中的 UUID v4（单用户，无服务端账号系统）。

---

### 资源适配器

当前只实现 `external_manual`，其余类型规划中但未实现（原生 MC / AI 评分由 QuizPanel 统一承载）。

| adapter_type | 交互方式 | 完成方式 | 状态 |
|-------------|---------|---------|------|
| `external_manual` | 新标签页打开外链 | 手动输入得分 / 点击「已完成」 | ✅ 已实现 |
| `native_quiz` | 平台内渲染 | 自动评分 | 未实现 |
| `ai_graded_text` | 平台内文字输入框 | Claude API 评分 | 未实现 |
| `observation` | 平台内 checklist | 全部勾选 | 未实现 |

PhET 实验步骤通过 `Resource.description` 字段在 ResourceRow 内展示，无需独立 ChecklistCard。

新增平台 = 加一个 `case` + 一个组件，其余代码不动。

---

### 完成信号（统一结构）

```typescript
interface CompletionResult {
  status: 'passed' | 'failed' | 'skipped'
  score?: number         // 实际得分
  score_max?: number     // 满分
  ai_feedback?: string   // AI 评分反馈文字
  evidence_url?: string  // 截图路径（可选）
}
```

Flow Controller 只读这个结构，对平台来源完全无感。

---

### Flow Controller 算法

```
function getNextAction(userId, week, day):

  if NOT day_unlocked(userId, week, day):
    return { action: 'DAY_LOCKED' }

  // 按 slot_order 遍历 A 层资源
  for resource in getResources(week, day, tier='A'):
    if NOT completed(userId, resource.id):
      return { action: 'PRESENT', resource }

  // 所有 A 层完成，评估门控
  khan_score = calcKhanScore(userId, week, day)

  if khan_score >= 0.75:
    unlock_next_day(userId, week, day)
    return { action: 'DAY_COMPLETE', score: khan_score }

  // 未达标，查 B 层补救
  failed_concepts = getFailedConcepts(userId, week, day)
  b_resources = getUnseenBResources(userId, failed_concepts)

  if b_resources not empty:
    return { action: 'SHOW_REMEDIATION', resources: b_resources }

  return { action: 'RETRY_A_LAYER' }
```

---

## 五b、每日挑战题（Quiz System）

### 设计原则

挑战题与 A 层学习流程**解耦**：完成 A 层后（COMPLETE 阶段）软触发，不影响解锁条件，不纳入 pass-rate 计算。目标是知识巩固和主动提取练习（retrieval practice），而非关口评分。

### 题型

| 题型 | 评分方式 | 比例 |
|------|---------|------|
| MCQ（单选）| 本地即时 | 混合 |
| fill（填空）| 本地即时 | 混合 |
| short（简答）| Claude AI | 少量 |
| feynman（费曼反思）| Claude AI | 每日1题 |

每日 3 普通 + 1 feynman = 4 题。客观题（MCQ/fill）点提交立即显示结果，不调 AI，不阻塞 UI。

### 选题算法

1. 按当天 A 层 `concepts[]` 筛选题库
2. 软去重：14 天内**答对**的题排除；答错的题始终保留再出（强化薄弱点）
3. 保证至少 1 道 difficulty-3 题（合成难度）
4. feynman 题：按 net failure score（失败概念 +1 / 通过 -1）选最弱概念对应题，附加近因惩罚（3 天内答过的题降权，避免反复刷同一题）

### FRQ 分数回填

独立于 Quiz，学生完成 FRQ 后手动录入得分/满分（≥60% 计通过）。回填数据通过 `FRQCompletion` 表存储，`week/day` 按当前页上下文写入。

---

## 五c、每日综合得分（Scoring）

### 设计目标

给每天的学习行为一个 0–100 的综合量化，在仪表盘 DayCell 右上角显示 badge，让学生对学习质量有直观感知。

### 计分公式

```
总分 = min(100, A层分 + B层加成 + C/FRQ加成 + Quiz分)

A层分（0–55）= completionRate × 30 + passRate × 25
B层加成（0–7）= bQuality × min(bGraded/3, 1) × 7
C/FRQ加成（0–8）= cQuality × min((cGraded + frqCount)/2, 1) × 8
Quiz分（0–30）= weightedCorrectRate^1.5 × 30
```

- `passRate`：使用 aTotal（而非已评分数）作为分母——跳过的资源计入惩罚，防止只做简单题绕过门控
- `bQuality / cQuality`：B/C 层已评分资源的通过率；FRQ ≥ 60% 得分算通过
- Quiz 使用 x^1.5 而非 x²：保留错题惩罚，但对 60–80% 正确率的正常学习阶段不过分苛刻

### Quiz 题目权重（影响加权正确率计算）

feynman=0.30 > difficulty-3=0.25 > difficulty-2=0.20 > 默认=0.15  
时效系数：7天内=1.0，30天内=0.85，更早=0.70（鼓励近期复习）  
重复作答取最新一次（`latestPerQuestion` 去重）

---

## 六、功能完成状态

| 功能 | 状态 | 说明 |
|------|------|------|
| 学习路径导航 | ✅ 完成 | Week 1–8，顺序解锁，75% 门控 |
| 外部资源执行 | ✅ 完成 | ExternalCard，手动输入得分/点完成 |
| 自适应 B 层 | ✅ 完成 | A 层 < 75% 自动展开 B 层 |
| 历年 FRQ 推荐 | ✅ 完成 | PDF 精准定位 + 道德审判弹窗 |
| FRQ 分数回填 | ✅ 完成 | 学生手动录入得分/满分，通过率 ≥ 60% |
| 每日挑战题（Quiz）| ✅ 完成 | MCQ/fill 即时评分，short/feynman AI 评分 |
| AI 追问 | ✅ 完成 | 答题后可向 AI 追问关于该题的问题 |
| 每日综合得分 | ✅ 完成 | 0–100 分，Dashboard badge 显示 |
| 每日 AI 反馈 | ✅ 完成 | 当天完成后调 Claude API 生成总结 |
| 进度仪表盘 | ✅ 完成 | 8×7 格，含完成状态 + 得分 badge |
| 只读分享链接 | ❌ V2 | 导出 JSON 作为临时替代 |
| 多设备同步 | ❌ V2 | 导出/导入 JSON |

---

## 七、MVP → V2 扩展路径

| 功能 | MVP | V2 扩展方式 |
|------|-----|-----------|
| 管道粒度 | 按 Day | resources 加 `kp_id`，Flow Controller 改为按 KP 循环 |
| 得分验证 | 手动输入 | 加 `adapter_type: screenshot_verify`，一个新组件 |
| FRQ 评分 | 不做 | 加 `adapter_type: ai_graded_image`，一个新组件 |
| 新平台接入 | — | switch 加一个 case，插入资源行 |
| 多课程 | — | resources / knowledge_points 加 `course_id` 字段 |
| 第二轮学习 | — | resource_completions 加 `round INT`，Flow Controller 加 round 参数 |

---

## 八、明确排除的方案

| 方案 | 排除原因 |
|------|---------|
| Pipeline 独立数据表 | 现有 week/day/slot_order 已足够表达顺序，额外表是过度设计 |
| LaunchSession 数据库表 | localStorage 足够处理「中途关闭」场景，无需落库 |
| 截图 AI 解析（MVP）| 截图布局多变，解析可靠性低；手动输入摩擦可接受 |
| AI 即时生成练习题 | 物理题数值一致性难以保证，无人工审核不适合 MVP |
| FRQ AI 评分（MVP）| 可靠性要求高，需要专门 prompt 设计和测试，推迟到 V2 |
| Adapter 正式接口/类 | switch + 组件已满足需求，接口是为了想象中的多人协作场景 |
| iframe 嵌入 OpenStax | 需验证 X-Frame-Options，不确定是否允许，外链更保险 |

---

## 九、待确认事项（V2）

- [x] 技术栈选型 → Next.js + Dexie.js + Claude API + Cloudflare Pages
- [x] OpenStax Test Prep → 由 Quiz System 统一承载，无需独立 native_quiz 适配器
- [x] AI 评分 → short/feynman 题型已实现，FRQ 评分排除至 V2
- [x] 分享链接安全策略 → MVP 导出 JSON 文件
- [x] OpenStax iframe 嵌入 → 排除，外链更保险
- [ ] Week 8 综合复习的自适应逻辑（基于 Week 1–7 弱点数据推荐）
- [ ] difficulty-3 题目补全（Units 2–8 各 unit 目前偏少）
